package org.ironlions.sovereign.pathfinding

/**
 * Declares the Environment Map containing all of the FieldObjects
 *
 * @param objects List of FieldObjects, these basically define what the map looks like
 */
class Environment(objects: List<FieldObject>) {
    private val objects: List<FieldObject> = objects
}